import 'package:flutter/material.dart';

class ListOrder extends StatelessWidget {
  const ListOrder({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Desenvolver lista de pedidos'),
    );
  }
}
